	    |-----handy-ruler------------------------------------------------------|
streamripper: streamripper
streamripper:
streamripper: Using information which a SHOUTcast server can optionally send,
streamripper: this program breaks an audio stream into "tracks," stores them in
streamripper: separate files as they arrive, and names the files by appending
streamripper: ".mp3" to the name of the track.  The tracks can be listened to at
streamripper: the user's leisure with an MPEG Layer 3 audio player.
streamripper:
streamripper:
streamripper:
streamripper:
	    |-----handy-ruler------------------------------------------------------|
streamripper: streamripper
streamripper:
streamripper: Using information which a SHOUTcast server can optionally send,
streamripper: this program breaks an audio stream into "tracks," stores them in
streamripper: separate files as they arrive, and names the files by appending
streamripper: ".mp3" to the name of the track.  The tracks can be listened to at
streamripper: the user's leisure with an MPEG Layer 3 audio player.
streamripper:
streamripper:
streamripper:
streamripper:
