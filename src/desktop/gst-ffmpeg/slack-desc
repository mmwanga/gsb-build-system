          |-----handy-ruler------------------------------------------------------|
gst-ffmpeg: gst-ffmpeg (GStreamer "ffmpeg" Plugins)
gst-ffmpeg:
gst-ffmpeg: GStreamer FFmpeg plug-in contains one plugin with a set of elements 
gst-ffmpeg: using the FFmpeg library code. It contains most popular decoders as 
gst-ffmpeg: well as very fast colorspace conversion elements.
gst-ffmpeg:
gst-ffmpeg: http://gstreamer.freedesktop.org/modules/gst-ffmpeg.html
gst-ffmpeg:
gst-ffmpeg:
gst-ffmpeg:
gst-ffmpeg:
