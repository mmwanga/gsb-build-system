         |-----handy-ruler------------------------------------------------------|
gst-pulse: gst-pulse (GStreamer Plugin for PulseAudio)
gst-pulse:
gst-pulse: GStreamer is a library that allows the construction of graphs of
gst-pluse: media-handling components, ranging from simple Ogg/Vorbis playback
gst-pulse: to complex audio (mixing) and video (non-linear editing) processing.
gst-pulse:
gst-pulse: This plugin allows GStreamer to the PulseAudio Sound Server.
gst-pulse:
gst-pulse:
gst-pulse:
gst-pulse:
gst-pulse:
