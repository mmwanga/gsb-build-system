      |-----handy-ruler------------------------------------------------------|
gnome-shell: gnome-shell (GNOME Shell)
gnome-shell:
gnome-shell: GNOME Shell acts as a compositing manager for the desktop, and
gnome-shell: displays both application windows and other objects in a Clutter 
gnome-shell: scene graph. The GNOME Shell is intended to replace functions
gnome-shell: handled by the GNOME Panel and by the window manager in previous 
gnome-shell: versions of GNOME. 
gnome-shell:
gnome-shell:
gnome-shell:
gnome-shell:
