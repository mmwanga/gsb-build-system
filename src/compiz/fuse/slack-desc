fuse: fuse (Filesystem In Userspace)
fuse:
fuse: FUSE makes it possible to implement a filesystem in a userspace program.
fuse: Features include: simple yet comprehensive API, secure mounting by non-root
fuse: users, support for 2.4 and 2.6 Linux kernels, and multi-threaded operation.
fuse:
fuse:
fuse:
fuse:
fuse:
fuse:
