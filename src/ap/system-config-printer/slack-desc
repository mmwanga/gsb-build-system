                     |-----handy-ruler------------------------------------------------------|
system-config-printer: system-config-printer (GNOME CUPS Configuration Utility)
system-config-printer: 
system-config-printer: system-config-printer is a printer utility to configure a CUPS server 
system-config-printer: (often the local machine) using the CUPS API. The tool is written in 
system-config-printer: Python, using pygtk for the graphical parts and with some Python 
system-config-printer: bindings (pycups) for the CUPS API.  It is largely the same as 
system-config-printer: using the CUPS web interface for configuring printers, but has 
system-config-printer: the advantage of being a native application rather than a web page. 
system-config-printer: 
system-config-printer: 
system-config-printer: 
