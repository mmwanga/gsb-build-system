      |-----handy-ruler------------------------------------------------------|
ffmpeg: ffmpeg (Record, convert, and stream audio and video)
ffmpeg:
ffmpeg: FFmpeg is a complete solution to record, convert and stream audio 
ffmpeg: and video. It includes libavcodec, the leading audio/video codec 
ffmpeg: library. libavcodec is a library containing all the FFmpeg audio
ffmpeg: and video encoders and decoders, and libavformat is a library 
ffmpeg: containing parsers and generators for all common audio/video 
ffmpeg: formats.
ffmpeg:
ffmpeg:
ffmpeg:
