      |-----handy-ruler------------------------------------------------------|
libmms: libmms (Microsoft streaming protocol parser library)
libmms:
libmms: LibMMS is common library for parsing mms:// and mmsh:// type network
libmms: streams. These are commonly used to stream Windows Media Video content
libmms: over the web. LibMMS itself is only for receiving MMS stream, it
libmms: doesn't handle sending at all. If you need streaming functionality,
libmms: look for other protocol, such as RT(S)P.
libmms:
libmms:
libmms:
libmms:
