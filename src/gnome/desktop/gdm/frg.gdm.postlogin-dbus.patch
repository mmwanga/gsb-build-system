--- config/PostLogin.org	Thu Aug 11 00:04:05 2005
+++ config/PostLogin	Thu Aug 11 00:05:23 2005
@@ -1,8 +1,11 @@
 #!/bin/sh
 #
-# Note: this is a sample and will not be run as is.  Change the name of this
-# file to <sysconfdir>/gdm/PostLogin/Default for this script to be run.  This
-# script will be run before any setup is run on behalf of the user and is
+# This script will be run before any setup is run on behalf of the user and is
 # useful if you for example need to do some setup to create a home directory
 # for the user or something like that.  $HOME, $LOGIN and such will all be
 # set appropriately and this script is run as root.
+
+if [ -x /usr/bin/dbus-daemon-1 ]
+then
+	/usr/bin/dbus-daemon-1 --session --fork
+fi
